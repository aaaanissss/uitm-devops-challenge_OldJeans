# task 6 - CI/CD Security Testing (Bonus)

name: CI Starter

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "✅ GitHub Actions is running for Task 6"
